- page_title!('Editando mix')

#region-mixes.container

  .region
    .region-title
      %h1
        Editando
        %b
          mix

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @mix, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
