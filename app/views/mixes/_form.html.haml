= form_for @mix, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @mix.errors.any?
    #error_explanation
      %h2= "#{pluralize(@mix.errors.count, "error")}"
      %ul
        - @mix.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :soundcloud_link, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :soundcloud_link, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
