- page_title!('Editando auspiciador')

#region-auspiciadores.container

  .region
    .region-title
      %h1
        Editando
        %b
          auspiciador

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @auspiciador, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
