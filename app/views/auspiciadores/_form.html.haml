= form_for @auspiciador, :html => { class: 'form-horizontal', role: 'fordb/migrate/20131119172026_create_diseños.rbm' } do |f|
  - if @auspiciador.errors.any?
    #error_explanation
      %h2= "#{pluralize(@auspiciador.errors.count, "error")}"
      %ul
        - @auspiciador.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :logo, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :logo, class: 'form-control'
  .form-group
    = f.label :link, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :link, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
