
#carousel-generic.carousel.evento

  %ol.carousel-indicators
    - @eventos.each_with_index do |evento, index|
      - if index == 0
        %li.active{:'data-target' => '#carousel-generic', :'data-evento-to' => index}
      - else
        %li{:'data-target' => '#carousel-generic', :'data-evento-to' => index}

  .carousel-inner
    - @eventos.each_with_index do |evento, index|
      - if index == 0
        .item.active
          - if @lista_evento_viernes
            = link_to 'Mi lista', lista_path(@lista_evento_viernes), class: 'btn-default yo-voy'
          - else
            = link_to 'Hacer lista', new_evento_lista_path(evento), class: 'btn-default yo-voy'
          = image_tag evento.flyer_url(:full)
          %h3.title
            = '+ ' + evento.titulo + ' /'
            = l evento.fecha, format: :supershort
      - else
        .item
          - if @lista_evento_viernes
            = link_to 'Mi lista', lista_path(@lista_evento_viernes), class: 'btn-default yo-voy'
          - else
            = link_to 'Hacer lista', new_evento_lista_path(evento), class: 'btn-default yo-voy'
          = image_tag evento.flyer_url(:full)
          %h3.title
            = '+ ' + evento.titulo + ' /'
            = l evento.fecha, format: :supershort
